#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

#include <stdio.h>

#ifdef PNUT_CC
typedef int FILE;
#endif

int main() {
  FILE *f;
  char c;
  f = fopen("tests/_all/six-cc-tests/fgetc.c", "r");
  while ((c = fgetc(f)) != -1) {
    putchar(c);
  }
  fclose(f);

  return 0;
}

